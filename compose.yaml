services:
  api:
    build:
      context: .
      dockerfile: src/Lca.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
    ports:
      - "${API_PORT:-8080}:8080"
